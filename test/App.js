import React from 'react';
import LoginForm from './LoginForm'; // LoginForm 컴포넌트를 불러옵니다.

function App() {
  return (
    // App 컴포넌트 자체에 별도 스타일링이 필요 없습니다.
    // body 스타일이 LoginForm을 중앙 정렬해줍니다.
    <LoginForm />
  );
}

export default App;